# 整数转罗马数字
## 题目

七个不同的符号代表罗马数字，其值如下：

```js
符号	值
I	1
V	5
X	10
L	50
C	100
D	500
M	1000
```
罗马数字是通过添加从最高到最低的小数位值的转换而形成的。将小数位值转换为罗马数字有以下规则：

如果该值不是以 4 或 9 开头，请选择可以从输入中减去的最大值的符号，将该符号附加到结果，减去其值，然后将其余部分转换为罗马数字。

如果该值以 4 或 9 开头，使用 减法形式，表示从以下符号中减去一个符号，例如 4 是 5 (V) 减 1 (I): IV ，9 是 10 (X) 减 1 (I)：IX。仅使用以下减法形式：4 (IV)，9 (IX)，40 (XL)，90 (XC)，400 (CD) 和 900 (CM)。

只有 10 的次方（I, X, C, M）最多可以连续附加 3 次以代表 10 的倍数。你不能多次附加 5 (V)，50 (L) 或 500 (D)。如果需要将符号附加4次，请使用 减法形式。

给定一个整数，将其转换为罗马数字。

 

### 示例 1：

输入：`num = 3749`

输出： `"MMMDCCXLIX"`

解释：

3000 = MMM 由于 1000 (M) + 1000 (M) + 1000 (M)
 700 = DCC 由于 500 (D) + 100 (C) + 100 (C)
  40 = XL 由于 50 (L) 减 10 (X)
   9 = IX 由于 10 (X) 减 1 (I)
注意：49 不是 50 (L) 减 1 (I) 因为转换是基于小数位

### 示例 2：

输入：`num = 58`

输出：`"LVIII"`
解释：

`50 = L`
 `8 = VIII`
### 示例 3：

输入：`num = 1994`

输出：`"MCMXCIV"`

解释：

1000 = M
 900 = CM
  90 = XC
   4 = IV

## 

这题是 **LeetCode · 整数转罗马数字（Integer to Roman）**，和你刚做的「罗马转整数」正好是**反向题**。
思路一句话：**从大到小“贪心”往下减**。

---

## ✅ 核心思路（贪心法，标准解）

罗马数字的规则已经把所有「合法的拼法」都限定死了，其实你只需要：

1. **把所有可能的数值列出来（包含减法形式）**
2. **按从大到小排序**
3. 每次：

   * 如果 `num >= 当前值`
   * 就拼接对应的罗马字符
   * `num -= 当前值`
4. 直到 `num === 0`

---

## ✅ 为什么一定要把 900、400、90、40、9、4 单独列出来？

因为：

* `IV、IX、XL、XC、CD、CM` **是合法但不能拆的最小单位**
* 一旦你只用 `I、V、X...`，就会出现非法情况（如 `IIII`）

所以我们**直接把“规则写进表里”**，代码反而更简单。

---

## ✅ JS 标准写法（最推荐）

```js
/**
 * @param {number} num
 * @return {string}
 */
var intToRoman = function(num) {
  // 1. 数值 + 罗马字符对（包含减法形式）
  const values = [
    1000, 900, 500, 400,
    100,  90,  50,  40,
    10,   9,   5,   4,
    1
  ];

  const symbols = [
    "M",  "CM", "D",  "CD",
    "C",  "XC", "L",  "XL",
    "X",  "IX", "V",  "IV",
    "I"
  ];

  let res = "";

  // 2. 贪心：从大到小
  for (let i = 0; i < values.length; i++) {
    while (num >= values[i]) {
      res += symbols[i];
      num -= values[i];
    }
  }

  return res;
};
```

---

## 🧠 示例拆解（`num = 1994`）

| num  | 匹配       | 结果      |
| ---- | -------- | ------- |
| 1994 | 1000 → M | M       |
| 994  | 900 → CM | MCM     |
| 94   | 90 → XC  | MCMXC   |
| 4    | 4 → IV   | MCMXCIV |
| 0    | 结束       | ✅       |

---

## ⏱️ 复杂度分析

* **时间复杂度**：`O(1)`

  * 因为最多只会拼接有限次（罗马数字长度 < 20）
* **空间复杂度**：`O(1)`

---

## 📌 和上一题的“对称关系”

| 题目          | 核心思想      |
| ----------- | --------- |
| Roman → Int | 左小右大 → 减  |
| Int → Roman | 从大到小 → 贪心 |

👉 **一加一减，正反闭环**，面试官非常喜欢你能把这两题一起讲清楚。

---

## 🧩 一句话总结

> **把所有合法的罗马“积木”列好，从大到小往里塞，直到 num 归零。**

如果你愿意，我可以：

* 给你 **极简版写法（数组对象合并）**
* 或 **对比错误写法（为什么会翻车）**
* 或 **把两题合在一起做成一个工具函数**

你选一个，我接着带你 🚀
