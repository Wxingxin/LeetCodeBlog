给定一个排序数组和一个目标值，在数组中找到目标值，并返回其索引。如果目标值不存在于数组中，返回它将会被按顺序插入的位置。

请必须使用时间复杂度为 O(log n) 的算法。

 

示例 1:

输入: nums = [1,3,5,6], target = 5
输出: 2
示例 2:

输入: nums = [1,3,5,6], target = 2
输出: 1
示例 3:

输入: nums = [1,3,5,6], target = 7
输出: 4
# 

这题是 **二分查找的“下界（lower bound）”模板题**（LeetCode 35）。
我会 **结合示例，一步一步走二分过程**，让你知道为什么答案是对的。

---

## 一、题目真正要你找什么？

不是“找没找到”，而是：

> **找第一个 ≥ target 的位置**

* 如果这个位置的值刚好等于 target → 它的索引
* 如果数组里没有 target → 这个位置就是插入位置

---

## 二、必须用 O(log n) ⇒ 二分查找

我们维护一个 **闭区间 [left, right]**：

* `left`：可能的插入位置
* `right`：搜索边界

### 初始化

```js
left = 0
right = nums.length - 1
```

---

## 三、结合示例一步一步走

### 示例

```txt
nums = [1,3,5,6]
target = 5
```

---

### 第 1 轮

```txt
left = 0, right = 3
mid = 1 → nums[1] = 3
```

* 3 < 5
  👉 target 在 **右边**

```txt
left = mid + 1 = 2
```

---

### 第 2 轮

```txt
left = 2, right = 3
mid = 2 → nums[2] = 5
```

* 5 ≥ 5
  👉 可能就是答案，但**左边也可能还有**

```txt
right = mid - 1 = 1
```

---

### 循环结束

```txt
left = 2, right = 1  → left > right
```

👉 此时 **left 就是插入位置**

```txt
返回 left = 2
```

---

## 四、为什么一定返回 left？

因为整个过程中我们维护的是：

> **left 永远指向第一个“可能 ≥ target”的位置**

* target 存在 → left 指向它
* target 不存在 → left 指向它该插入的位置

---

## 五、代码（标准模板，可直接背）

```js
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var searchInsert = function(nums, target) {
  let left = 0;
  let right = nums.length - 1;

  while (left <= right) {
    const mid = left + ((right - left) >> 1);

    if (nums[mid] >= target) {
      right = mid - 1;
    } else {
      left = mid + 1;
    }
  }

  return left;
};
```

---

## 六、再用一个“不存在”的例子强化理解

```txt
nums = [1,3,5,6]
target = 2
```

过程结束后：

```txt
left = 1
```

插入后数组：

```txt
[1, 2, 3, 5, 6]
     ↑
```

---

## 七、二分查找一句话口诀（非常重要）

> **找插入位置，用“第一个 ≥ target”，循环结束返回 left**

---

如果你愿意，我可以下一步帮你：

* 对比 **找左边界 / 右边界的二分模板**
* 或讲清楚 **为什么不用 return mid**
* 或把 **所有二分查找题型做一个总结表**

你直接说 👍
