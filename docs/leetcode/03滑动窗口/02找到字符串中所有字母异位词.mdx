import ProblemHeader from '../../components/header/ProblemHeader'
import Main from './main/a2.md'

<ProblemHeader
  title="找到字符串中所有字母异位词"
  difficulty="medium"
  tags={["哈希表", "字符串","滑动窗口"]}
  meta={<span>LeetCode 438</span>}
>

给定两个字符串 `s` 和 `p`，找到 `s` 中所有 `p` 的 异位词 的子串，返回这些子串的起始索引。

:::info 信息

不考虑答案输出的顺序。

:::
</ProblemHeader>

<Main />
