import ProblemHeader from "../../components/header/ProblemHeader";
import Main from "./main/a4.md";

<ProblemHeader
  title="接雨水"
  difficulty="hard"
  tags={["栈", "数组",'双指针','动态规划','单调栈']}
  meta={<span>LeetCode 42</span>}
>
  给定` n `个非负整数表示每个宽度为` 1 `的柱子的高度图，计算按此排列的柱子，下雨之后能接多少雨水。
</ProblemHeader>

<Main />