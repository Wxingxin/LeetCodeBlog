# 题
- 给你一个整数数组 nums ，数组中的元素 互不相同 。返回该数组所有可能的子集（幂集）。

- 解集 不能 包含重复的子集。你可以按 任意顺序 返回解集。

 

## 示例 1：

输入：nums = `[1,2,3]`
输出：`[[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]]`
## 示例 2：

输入：nums = `[0]`
输出：`[[],[0]]`

# 解

下面给你 **最清晰、最标准的回溯生成子集（幂集）写法**，并结合示例解释思路。

---

# 🧠 思路：回溯（子集树 / 组合树）

子集问题与全排列不同：

* **子集不要求所有元素都必须用到。**
* **在每一层递归中，都可以选择加入当前元素或不加入。**

常见做法：

> 在回溯过程中，每一次进入递归，都把当前路径 `path` 加进结果 `res`。

递归参数 `(start)` 代表：
“从 `nums[start]` 这个位置开始，尝试选下一个元素”。

这样也能保证不会产生重复子集。

---

# ✅ JavaScript 代码（回溯法）

```js
/**
 * @param {number[]} nums
 * @return {number[][]}
 */
var subsets = function(nums) {
    const res = [];
    const path = [];

    const backtrack = (start) => {
        // 每到达一层，就把当前 path 加入结果
        res.push(path.slice());

        // 从 start 开始尝试加入新的元素
        for (let i = start; i < nums.length; i++) {
            path.push(nums[i]);        // 选择 nums[i]
            backtrack(i + 1);          // 递归下一层，下一层只能从 i+1 往后选
            path.pop();                // 撤销选择
        }
    };

    backtrack(0);
    return res;
};
```

---

# 🔍 用例：nums = [1, 2, 3] 的搜索树

### 初始：path = []

加入结果：`[]`

```
        []
    /     |     \
  [1]    [2]    [3]
 /  \     |       \
[1,2][1,3][2,3]   [3]
  |
[1,2,3]
```

生成顺序大致如下：

1. `[]`
2. `[1]`
3. `[1,2]`
4. `[1,2,3]`
5. `[1,3]`
6. `[2]`
7. `[2,3]`
8. `[3]`

输出：

```js
[
  [], [1], [2], [1,2], [3], [1,3], [2,3], [1,2,3]
]
```

---

# 🎉 小总结

* 回溯模板非常简洁：进入每层递归就保存当前 `path`
* `start` 参数保证不会出现重复子集
* 时间复杂度：O(n * 2^n)，因为总共有 2^n 个子集

---

如果你愿意，我可以继续给你：

* **更直观的图形化 DFS 扩展过程**
* **位运算法（位掩码）生成所有子集**
* **子集 II 版本（含重复数字的去重技巧）**

告诉我即可！
