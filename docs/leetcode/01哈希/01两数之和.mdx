import JsPlayground from "../../components/js-playground/index";
import ProblemHeader from "../../components/header/ProblemHeader";
import Main from "./main/o1.md";
import { o101 } from "./code/o1.problem.ts";

<ProblemHeader
  title="两数之和"
  difficulty="easy"
  tags={["哈希表", "数组"]}
  meta={<span>LeetCode 1</span>}
>
给定一个整数数组 `nums` 和一个整数目标值 `target`，

请你在该数组中找出 和为目标值 `target` 的那 两个 整数，

并返回它们的数组下标。

:::info

你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。

你可以按任意顺序返回答案。

:::

</ProblemHeader>

<Main />

<JsPlayground problem={o101} height={300} />
