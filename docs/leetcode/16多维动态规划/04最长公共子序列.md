# 题
给定两个字符串 text1 和 text2，返回这两个字符串的最长 公共子序列 的长度。如果不存在 公共子序列 ，返回 0 。

一个字符串的 子序列 是指这样一个新的字符串：它是由原字符串在不改变字符的相对顺序的情况下删除某些字符（也可以不删除任何字符）后组成的新字符串。

例如，"ace" 是 "abcde" 的子序列，但 "aec" 不是 "abcde" 的子序列。
两个字符串的 公共子序列 是这两个字符串所共同拥有的子序列。

 

## 示例 1：

输入：`text1 = "abcde", text2 = "ace" `

输出：`3`

解释：最长公共子序列是 `"ace"` ，它的长度为 `3` 。

## 示例 2：

输入：`text1 = "abc", text2 = "abc"`

输出：`3`

解释：最长公共子序列是 `"abc"` ，它的长度为 `3` 。
## 示例 3：

输入：`text1 = "abc", text2 = "def"`

输出：`0`

解释：两个字符串没有公共子序列，返回 `0` 。

# 解

下面是 LeetCode 1143「最长公共子序列（LCS）」：经典二维 DP。

核心：`dp[i][j]` 表示 **text1 前 i 个字符**（下标 0..i-1）和 **text2 前 j 个字符**（下标 0..j-1）的最长公共子序列长度。

转移：

* 如果 `text1[i-1] === text2[j-1]`：说明这个字符可以接在公共子序列后面
  `dp[i][j] = dp[i-1][j-1] + 1`
* 否则：不能同时用这两个结尾字符，只能“丢掉一个”
  `dp[i][j] = max(dp[i-1][j], dp[i][j-1])`

---

## 代码（带详细注释）

```js
/**
 * @param {string} text1
 * @param {string} text2
 * @return {number}
 */
var longestCommonSubsequence = function(text1, text2) {
  const m = text1.length;
  const n = text2.length;

  // dp[i][j]：text1 前 i 个字符 与 text2 前 j 个字符 的 LCS 长度
  // i, j 从 0 开始表示“空串”，所以 dp 尺寸是 (m+1) x (n+1)
  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));

  // 遍历 text1 的前 i 个字符
  for (let i = 1; i <= m; i++) {
    // 遍历 text2 的前 j 个字符
    for (let j = 1; j <= n; j++) {
      // 当前比较的字符分别是 text1[i-1] 和 text2[j-1]
      if (text1[i - 1] === text2[j - 1]) {
        // 字符相等：LCS 可以在 dp[i-1][j-1] 基础上 +1
        dp[i][j] = dp[i - 1][j - 1] + 1;
      } else {
        // 字符不等：取“丢 text1 一个字符” 或 “丢 text2 一个字符” 的最大值
        dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
      }
    }
  }

  // 右下角就是最终答案：text1 全部 & text2 全部 的 LCS 长度
  return dp[m][n];
};
```

---

## 结合示例 1 详细推演

`text1 = "abcde"`，`text2 = "ace"`

我们要找公共子序列（保持相对顺序即可，不要求连续）。

### 关键匹配点

* `a` 能匹配 `a`
* `c` 能匹配 `c`
* `e` 能匹配 `e`
  所以 LCS = `"ace"`，长度 3。

### DP 是怎么得到 3 的？

把 `text1` 放行，`text2` 放列（这里展示核心过程）：

* 当 i=1 比较到 `a`，j=1 比较到 `a`，相等
  `dp[1][1] = dp[0][0] + 1 = 1`
* 继续往后，遇到 `c` 对 `c` 相等
  `dp[3][2] = dp[2][1] + 1 = 2`
* 最后 `e` 对 `e` 相等
  `dp[5][3] = dp[4][2] + 1 = 3`

不相等的时候，dp 会“继承”上方或左方的较大值，相当于尝试跳过某些字符，但不改变顺序。

---

## 示例 2

`"abc"` 和 `"abc"`：每个字符都能一一匹配
最终 `dp[3][3] = 3`

---

## 示例 3

`"abc"` 和 `"def"`：没有任何字符相等
dp 全程都是 0，最后返回 0

---

如果你还想进一步省空间（把二维 dp 压成一维 `O(n)`），我也可以给你一版同样“带注释 + 结合案例推演”的写法。
